import * as types from '../constants/ActionTypes';
import _ from 'lodash';

const initialState = {};

export function forecast(state = {}, action = null) {
  console.log(action);
  switch (action.type){

  }
}

